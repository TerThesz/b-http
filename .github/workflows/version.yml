name: version-control
on:
  pull_request:
    branches:
      - staging
jobs:
  check-version:
    runs-on: ubuntu-latest
    steps:
      - id: version
        uses: EndBug/version-check@v1
        with:
          file-url: https://unpkg.com/your-http@1.3.2/package.json
          static-checking: localIsNew
      - if: steps.check.outputs.changed == 'false'
        run: exit 69
        